<Window x:Class="Aldune.Windowing.SyncConflictsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:res="clr-namespace:Aldune.Resources"
        Title="{x:Static res:Strings.SyncConflictTitle}"
        Width="560" Height="500" MinWidth="440" MinHeight="320"
        WindowStyle="None" ResizeMode="CanResize" Background="#2A261F"
        Topmost="True" StateChanged="OnWindowStateChanged">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="34" ResizeBorderThickness="6" GlassFrameThickness="-1" CornerRadius="0" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <Style x:Key="ConflictButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#EDE7DC" />
            <Setter Property="Background" Value="#3C3730" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="9,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BackgroundBorder" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#4E4840" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#5F584E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ConflictDangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ConflictButtonStyle}">
            <Setter Property="Foreground" Value="#E8A0A0" />
            <Setter Property="Background" Value="#5A2A28" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,18,14,14">
            <!-- El margen derecho reserva el sitio de los botones: el aviso es largo y, sin esto, el
                 texto se dibujaba por debajo de ellos (los botones van encima y lo tapaban). -->
            <StackPanel Margin="0,0,140,0">
                <TextBlock Text="{x:Static res:Strings.SyncConflictTitle}" Foreground="#F5F0E6"
                           FontSize="17" FontWeight="SemiBold" />
                <TextBlock Text="{x:Static res:Strings.SyncConflictHint}" Foreground="#A79E90"
                           FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                <!-- La cabecera es zona de arrastre (CaptionHeight): sin IsHitTestVisibleInChrome el
                     clic llegaría al arrastre en vez de al botón, igual que ya pasa en NoteWindow y
                     SettingsWindow. -->
                <Button Content="{x:Static res:Strings.SyncConflictDismissAll}"
                        Click="OnDismissAllClick" Margin="0,0,8,0"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        Style="{StaticResource ConflictDangerButtonStyle}" />
                <Button Content="&#x2715;" Width="28" Height="28"
                        Click="OnCloseClick"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        Style="{StaticResource ConflictButtonStyle}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="20,0,20,20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ConflictsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#332F28" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{Binding LosingTitle}" Foreground="#F5F0E6"
                                               FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Row="1" Text="{Binding Details}" Foreground="#A79E90"
                                               FontSize="11" TextWrapping="Wrap" Margin="0,4,0,9" />
                                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                                        <Button Content="{x:Static res:Strings.SyncConflictRestore}"
                                                Tag="{Binding Conflict.Id}" Click="OnRestoreClick"
                                                Style="{StaticResource ConflictButtonStyle}" />
                                        <Button Content="{x:Static res:Strings.SyncConflictDismiss}"
                                                Tag="{Binding Conflict.Id}" Click="OnDismissClick" Margin="8,0,0,0"
                                                Style="{StaticResource ConflictDangerButtonStyle}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <TextBlock x:Name="EmptyState" Text="{x:Static res:Strings.SyncNoConflicts}"
                       Foreground="#6B6459" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Collapsed" />
        </Grid>
    </Grid>
</Window>
